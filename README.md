# Arcade Controller Viewer

Simple XInput arcade controller viewer for Windows.

## Build

Install MSVC and run `build.cmd`.

## How to Use

1. run built `acv.exe`.
2. press (up to 8) buttons to register mapping.
